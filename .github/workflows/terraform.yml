name: "Terraform EC2 Deployment"

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch: # ✅ Adds a manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug Secrets
        env:
          NOT_SECRET_KEY: ${{ secrets.NOT_SECRET_KEY }}
        run: |
          echo "NOT_SECRET_KEY=${{ secrets.NOT_SECRET_KEY }}"
